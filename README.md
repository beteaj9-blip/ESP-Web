# ESP-Web

Knives Out Web ESP overlay (Next.js).

- `/[session]` page shows the ESP overlay driven by relay frames
- `NEXT_PUBLIC_WS_URL` env: relay WebSocket URL (default `ws://<hostname>:8080`)

Run: `npm install`, then `npm run dev` or `npm run build && npm start`
